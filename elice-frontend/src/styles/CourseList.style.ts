import { styled } from 'styled-components'

export const CourseCountDiv = styled.div`
  display: flex;
  justify-content: flex-start;
  padding: 10px 0;
  margin: 10px 0;
  border-bottom: 1px solid #e1e2e4;
`

export const Count = styled.a`
  font-weight: bold;
  font-size: 14px;
`

export const CourseListDiv = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
`
